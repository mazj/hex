
--p + d++ * not c * (dd - e)        /* --p => DEC_OP expr => expr
                                     * d++ => expr INC_OP => expr
                                     * not c => NOT expr => expr
                                     * d++ * not c => expr MUL_OP expr => expr
                                     * dd - e => expr MINUS_OP expr => expr
                                     * (dd - e) => LPAREN expr RPAREN => expr
                                     * (d++ * not c) * (dd - e) => expr MUL_OP expr => expr
                                     * (--p) + (d++ * not c * (dd - e)) => expr PLUS_OP expr => expr => expr_list_ => simple_stmt
                                     */